[phases.setup]
nixPkgs = ["python310", "postgresql"]

[phases.install]
cmds = ["cd backend && pip install -r requirements.txt"]

[phases.build]
cmds = ["cd backend && python init_database.py"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
